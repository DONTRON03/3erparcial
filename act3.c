#include <stdio.h>
#define T "tambor de hojalata"
int main ()
{
    char st[21]="todo puede hacerse";
    puts(T);
    puts("Permiso para salir en la foto");
    puts(st);
    puts(&st[8]);
    return 0;
}
